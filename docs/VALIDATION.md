# 验证记录

## 1.1.15 — 2026-09-24

图形安装器的真实安装按钮处理逻辑已验证会使用编辑后的路径；路径框可编辑，浏览按钮可用，成功后锁定选择并显示完成状态。独立临时目录中完成中文／空格路径、新建目录、已有空目录、相同包重复安装及逐文件 SHA-256 核对；其他非空目录、改动过的安装、相对路径、磁盘根目录、无效名称及文件路径均被拒绝并保留原内容。

使用临时目录的访问权限验证不可写位置：安装失败，无部分文件残留，随后恢复测试目录权限。自定义路径的实际 Windows 快捷方式目标、工作目录及旧快捷方式备份通过验证。所有检查复用正式安装器代码；快捷方式与文件仅写入本次创建的测试目录，未切换日常安装或中断计时。安装器预览已实际渲染检查，正式包包含英文与中文 README，旧版包校验仍兼容。

系统文件夹选择器的物理鼠标操作、其他磁盘与不同显示缩放下的实际交互未在本轮逐项实机验证。现有计时逻辑、控件渲染和便携／网站包白名单与哈希检查通过；不将这些检查作为安全软件误报已解决的证据。

## 1.1.14 — 2026-09-22

项目整理后重新通过正式应用构建、领域与音频逻辑验证、WPF 控件渲染、安装器内嵌载荷验证、便携包及网站包白名单／SHA-256 校验。网站包只包含当前版本下载，逐文件清单与部署校验脚本一致。Chrome 浏览器检查通过：桌面和 320／390／768 像素布局、动画章节／重播／Esc、键盘预设、实际下载、减少动态效果及文字放大。服务器部署模板未在本轮连接真实服务器验证。

渲染检查0–9和冒号的完整字形表，以及125 DIP浅／深背景读数。数字加宽并纵向压低后，维持固定字距和视觉中心；小时／分钟切换、只读显示及取消恢复检查通过。字形表改为按实际布局尺寸取样，避免预览拉伸字形。

## 1.1.13 — 2026-09-22

实际渲染0–9与冒号完整字形表，以及125 DIP浅深背景镂空读数；小时／分钟边界、取消和提醒切换继续通过。圆角数字使用内置矢量路径，字体无需安装，保留数字文本辅助功能名称。

## 1.1.12 — 2026-09-22

使用实际WPF字形路径绘制镂空内壁与内部淡白光，检查浅／深背景125 DIP渲染、分钟与小时切换、取消恢复编辑以及提醒隐藏读数。没有使用全层模糊或外发光滤镜，字形缓存只在读数或尺寸变化时失效；矢量控件保留文本辅助功能名称。

## 1.1.11 — 2026-09-22

WPF离屏渲染检查配置面板、125 DIP专注数字与浅深背景对照。专注读数无Effect，小时按需折叠；已有时分边界与预设保留检查通过。面板声音开关与偏好同步，编辑底板仍只在编辑时显示。

独立桌面流程通过：面板实际显示与置顶、专注中操作状态、声音开关即时保存且不关闭面板、预设切换、Esc关闭、重新打开及通过新面板正常退出。专注缩放、位置保存、恢复与提醒中断检查通过。未将程序动作验证等同于物理鼠标操作或多显示器混合DPI验收。

## 1.1.10 — 2026-09-22

三段内嵌CC0柔软碰撞素材已通过PCM格式、长度、有限值、首尾归零、直流偏移、能量和峰值检查；投出音75–85ms，落地约89–117ms。持续混音、优先碰撞、过期声音丢弃及即时停止仍通过。桌面实机音频输出就绪，采样21次投出及19次触地事件，静音、恢复及停止回收通过；动画采样269帧，平均17.15ms，最大28.96ms。

`build/throw-impacts.wav` 为6.5秒实际物理事件与同一混音器生成的试听。新音色使用Kenney柔软接触拟音素材，未将波形或设备检查等同于真人听感验证；“可爱／真实”的主观效果需要用户试听确认。来源和处理记录见 `assets/audio/README.md`。

## 1.1.9 — 2026-09-22

桌面流程在专注过程中主动改变番茄位置，验证到时仍保持移动后的右上锚点与顶部位置（DPI舍入误差小于1.5 DIP），恢复大小存在连续中间帧，不再跳到显示器角落。对比实际果蒂与发射点的屏幕坐标，误差小于2物理像素。投出22次、碰撞21次的声音事件、拖动停止、预览、静音恢复及取消编辑均通过。当前机器本次动画采样282帧，平均17.01ms，最大23.67ms；不代表其他设备性能。

## 1.1.8 — 2026-09-22

实际WPF渲染检查专注时编辑底板（含边框与高亮）及菜单均隐藏，数字与冒号正常显示；取消后编辑底板和菜单恢复。检查了小时／分钟边界和浅、深背景125 DIP预览。字迹采用暖色渐变与无位移轮廓，不增加单独的时间面板。此前的尺寸、透明度、置顶和位置保存验证继续适用。

## 1.1.7 — 2026-09-22

独立桌面验证逐帧检查画面的右边界和上边界：当前显示缩放下最大误差0.800逻辑像素，涵盖向右上角缩小、快速取消再开始和恢复原位。460ms缩放与透明过渡具有同步中间帧，完成后窗口与内容均收紧到125 DIP。动画结束使用一次原生位置和尺寸更新，避免多个窗口属性更新造成收尾跳动。

已验证展开位置的保存、隐藏恢复、取消后尺寸／原色／预设恢复，以及提醒中断过渡；仍保持32%果实不透明度及置顶。性能效果以当前机器验证为限，未声称所有硬件均保持固定帧率。

## 1.1.6 — 2026-09-22

已通过独立的专注桌面验证：实际窗口与内容均为125 × 125逻辑像素（允许系统DPI舍入不足1 DIP），逐帧观察到缩小与淡出的同步中间状态，果实最终32%不透明度，窗口保持置顶。隐藏再显示、快速取消后重新开始、恢复250 DIP编辑尺寸与原色、提醒中断缩小动画均通过；预设时长保留。

WPF离屏渲染检查小尺寸倒计时及浅／深背景对比。125 DIP中倒计时有效字号为16 DIP，取消后恢复原编辑界面。完整逻辑验证、发布ZIP文件白名单及SHA-256检查通过。置顶遵循Windows常规窗口规则，不覆盖系统安全桌面。

## 1.1.4 — 2026-09-22

专注状态仅将番茄图像降至48%不透明度，倒计时及控件不受父级透明度影响。已有编辑／计时／提醒渲染流程通过，并额外输出浅色、深色背景对照 `build/tomato-focus-backgrounds.png`，检查倒计时可读性和提醒状态恢复原色。正常显示时采用220ms渐变，关闭系统动画或离屏渲染时直接应用目标状态。

## 1.1.3 — 2026-09-22

- 首次触地与二次触地各触发一次事件，接触高度正确，第二次力度更小；落地停留后回收。
- 六组拟音检查有效能量、首尾归零、直流偏移与峰值。
- 左右声像、并发叠加而非互相打断、16声部上限、碰撞优先、软限幅、停止清空及过期事件丢弃通过。
- 显式传入无效音频设备验证失败退出，计时与动画不依赖音频设备成功。
- 投掷音效与到时提示音偏好独立保存；旧设置兼容。开发预览生成6.5秒立体声 `build/throw-impacts.wav`，用于实机听感确认。
- 本机实际音频输出设备就绪，桌面测试采样22次投出和21次碰撞事件；预览、即时静音、静音时继续动画、恢复音效、拖动停止与预览结束回收均通过，无新增音频错误日志。
- 加入声音后的桌面采样277帧，平均17.07ms（约59FPS），最大49.24ms，应用更新平均0.091ms，粒子峰值15。此为本机短时采样，不代表所有设备的性能或端到端音频延迟；音色真实感仍需常用音箱／耳机试听。

## 1.1.2 — 2026-09-22

- 30／60／120Hz 采样的阻尼运动与解析结果一致；一格停靠回落不越过相邻格、不重复发声，快速反向后收敛。
- 三种 40ms PCM 均验证首尾归零、峰值与响度范围、尾音快速衰减和低直流偏移；已有逐格限频与静音测试继续通过。
- 编辑／专注／提醒界面渲染通过，移除可见单位标签，保留只读倒计时、数字边界和取消后预设恢复检查。
- 新音色为合成设计，最终齿轮手感及耳机／扬声器听感仍以实机体验为准；可试听开发产物 `build/wheel-detents.wav`。

## 1.1.1 — 2026-09-22

新增编辑区只读倒计时验证：时／分边界（01:01:01、01:00:00、00:59:59、00:01:00、00:00:59、00:00:00）、计时读数不覆盖原预设、拨轮隐藏且禁用、取消后恢复编辑、到期提醒隐藏倒计时。编辑态／计时态／提醒态均通过实际 WPF 离屏渲染。

桌面流程验证启动后保持原位显示、数字随真实计时递减、重复开始不重置、到期动画、停止提醒以及取消后保留原时长。1.1.0 的“启动后隐藏”是历史行为，1.1.1 已改为原位显示计时。

## 1.1.0 — 2026-09-22

当前 Windows 10 x64 上已通过脚本构建、逻辑验证、真实 WPF 渲染和桌面流程。新增验证覆盖：

- 旧静音迁移、拨轮音效独立保存、旧倒计时字段保留。
- 水平／垂直摇晃、负屏幕坐标、幅度与时间门槛、单次触发、手势重置；普通拖动、轻微抖动和慢速往返不取消。
- 可见数字跨格、正反向循环、高速跨格合并、50ms 反馈限频、程序设置和吸附保持静音；24ms PCM 长度和无削波检查。
- 实际 WPF 滚轮事件经过动画后触发反馈，数值输入反馈一次，计时中普通及辅助输入均只读。
- 250 DIP 窗口在 100%／125%／150%／200% 下离屏渲染；编辑态和提醒态检查。离屏检查不替代真实多屏混合 DPI 验收。
- 2 秒计时隐藏／到期显示、拖动停止对应动作、8 秒预览停止、托盘显示保持计时、取消恢复编辑。

本轮桌面动画采样：279 帧，平均帧间隔 17.05ms（约 59 FPS），最大 21.11ms，每帧应用更新 0.053ms，粒子峰值 14，WPF 渲染层级 2。仅代表本机短时运行，不保证所有机器稳定 60 FPS。

本机 Windows 10 缺少新触觉接口，降级正常；真实震动效果未验证。音效波形和播放调用已检查，耳机／扬声器主观听感、实体鼠标摇晃与触屏手感仍需用户体验确认。桌面流程包含真实控件事件，但不替代物理鼠标自动化。未进行第三方杀毒样本提交，不能据此认定 360 告警已解决。

## 1.0.1 项目规范化

正式应用与验证工具已拆分为独立程序集。标准入口为 `./build.ps1 -Test`、`./build/Tomato.Verify.exe --render-preview`、`./build/Tomato.Verify.exe --smoke-test`。发布包内容与 SHA-256 由 `scripts/check-package.ps1` 检查。以下性能数据来自 1.0.0 的桌面短时采样，不作为 1.0.1 的重新测量结果。

本地脚本构建、独立逻辑验证、编辑态／提醒态渲染及 ZIP 白名单与 SHA-256 校验均已通过。本机缺少 .NET Framework 4.8 目标包，解决方案构建由 GitHub Windows runner 验证：[首次完整 CI](https://github.com/Dante9k/tomato-focus/actions/runs/35681183305) 已通过 Visual Studio 解决方案构建、脚本构建、逻辑验证、实际控件渲染、格式检查和打包校验。后续提交以对应 CI 运行结果为准。

验证日期：2026-09-22。平台：当前 Windows x64 桌面，屏幕 1536 × 864，WPF 渲染层级 2。

## 已验证

- 构建通过，无编译警告；不需要下载第三方运行依赖。
- 拒绝零时长与超过 23:59:59 的时间。
- 到期前不触发，到期只触发一次；剩余秒数向上取整。
- 模拟休眠跨过截止时刻后，正确进入提醒。
- 未到期重启恢复、已到期重启恢复、取消回到编辑状态。
- 状态文件原子覆盖、损坏 XML 回退默认值。
- 抛物线与解析公式相符；落地反弹、寿命回收。
- 时间数字循环与吸附。
- 连续 120 秒物理模拟，活动粒子数量不超过 36 个。
- 实际 WPF 编辑界面与提醒界面离屏渲染，并检查图片的文字、间距、透明背景和质感。
- 自动桌面流程：2 秒计时、主窗口隐藏、到期主窗口出现、实际动画绘制、通过拖动对应的停止动作回收动画、8 秒预览自动停止、取消后恢复编辑。

## 实际动画测量

首次绘图方式平均帧间隔 26.39 ms。改为保留每个番茄的绘图内容、只更新变换矩阵后，测得：

| 指标 | 测量值 |
| --- | --- |
| 采样帧数 | 249 |
| 平均帧间隔 | 17.23 ms，约 58 FPS |
| 采样期间最大帧间隔 | 42.85 ms |
| 每帧应用更新耗时 | 0.068 ms |
| 活动粒子峰值 | 15 |

这是当前机器约 4.3 秒的短时采样，包含真实透明桌面窗口，不能推断所有设备均稳定 60 FPS。应用更新耗时不包含 Windows 合成与显示耗时。

## 验收边界

桌面流程测试调用应用动作并创建真实窗口，不是鼠标输入自动化。正式企业分发前，应继续在目标硬件验证：

- 实际鼠标双击绿蒂、拖动果身、滚轮惯性以及键盘输入的手动体验。
- 全局 Esc 被其他程序占用时的备用停止方式。
- 多显示器正负坐标与混合 100% / 150% / 200% 缩放。
- 真实系统休眠与恢复、长时间计时和连续提醒的资源占用。
- 屏幕阅读器、触控输入、远程桌面与低性能 GPU。
- 企业代码签名与分发流程。
