# 安全说明

当前维护 1.0.x 版本。应用不发起网络请求、不上传用户数据、不需要管理员权限、不修改开机启动项。

状态与错误日志保存在当前 Windows 用户的 `LocalApplicationData/TomatoFocus` 目录。提供问题报告前，请自行检查日志中的本机路径或系统信息。

不要在公开 Issue 中发布漏洞利用细节、令牌、个人路径或其他敏感资料。若仓库提供 **Security → Report a vulnerability**，使用该入口；否则先通过不含敏感信息的 Issue 请求维护者提供私密报告渠道。请勿在找到私密渠道前上传完整漏洞材料。

当前便携程序未进行企业证书签名。SHA-256 文件用于校验下载完整性，不能替代代码签名。仅从维护者控制的仓库或构建记录获取程序。
